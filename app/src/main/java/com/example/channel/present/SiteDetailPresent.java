package com.example.channel.present;

public interface SiteDetailPresent {

    void showSiteDetail(String task_id, String pid, boolean isShowLoad);
    void delSite(String task_id, boolean isShowLoad);
}
